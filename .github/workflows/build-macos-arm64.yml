name: Build native Sherlock for macOS Apple Silicon

on:
  push:
    branches:
      - codex/macos-arm64-build
    paths:
      - .github/workflows/build-macos-arm64.yml

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project and packager
        run: |
          python -m pip install . pyinstaller
          python -m pip install --force-reinstall --no-binary tomli tomli==2.2.1

      - name: Build standalone executable
        run: |
          test "$(uname -m)" = "arm64"
          python -m PyInstaller \
            --noconfirm \
            --clean \
            --onefile \
            --name sherlock \
            --collect-data sherlock_project \
            sherlock_project/__main__.py
          chmod +x dist/sherlock
          codesign --force --deep --sign - dist/sherlock

      - name: Smoke test
        run: |
          file dist/sherlock | tee dist/build-info.txt
          file dist/sherlock | grep -q arm64
          ./dist/sherlock --help > dist/help.txt
          shasum -a 256 dist/sherlock > dist/sherlock.sha256

      - uses: actions/upload-artifact@v4
        with:
          name: sherlock-macos-arm64
          path: |
            dist/sherlock
            dist/sherlock.sha256
            dist/build-info.txt
            dist/help.txt
          if-no-files-found: error
          retention-days: 7
